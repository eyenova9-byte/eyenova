<nav class="mobile-bottom-nav-bar" id="mobileBottomNav" aria-label="Mobile Bottom Navigation">
  <div class="bottom-nav-grid">
    <!-- Tab 1: Home -->
    <a href="/" class="bottom-nav-tab {% if request.path == '/' %}bottom-nav-tab-active{% endif %}">
      <div class="bottom-nav-icon">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
        </svg>
      </div>
      <span class="bottom-nav-text">Home</span>
      {% if request.path == '/' %}
        <span class="bottom-nav-dot"></span>
      {% endif %}
    </a>

    <!-- Tab 2: Categories / Catalog -->
    <a href="/collections/all" class="bottom-nav-tab {% if request.path contains '/collections' %}bottom-nav-tab-active{% endif %}">
      <div class="bottom-nav-icon">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/>
        </svg>
      </div>
      <span class="bottom-nav-text">Categories</span>
      {% if request.path contains '/collections' %}
        <span class="bottom-nav-dot"></span>
      {% endif %}
    </a>

    <!-- Tab 3: Bag with Cart Counter -->
    <button type="button" class="bottom-nav-tab" data-cart-toggle aria-label="Shopping Bag">
      <div class="bottom-nav-icon relative-badge-wrap">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/>
        </svg>
        <span class="bottom-cart-badge" id="bottomCartCountBadge">{{ cart.item_count }}</span>
      </div>
      <span class="bottom-nav-text">Bag</span>
    </button>

    <!-- Tab 4: WhatsApp Concierge -->
    <a href="https://wa.me/97455123456" target="_blank" rel="noopener noreferrer" class="bottom-nav-tab">
      <div class="bottom-nav-icon">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M7.9 20A9 9 0 1 0 4 16.1L2 22Z"/>
        </svg>
      </div>
      <span class="bottom-nav-text">Concierge</span>
    </a>
  </div>
</nav>
